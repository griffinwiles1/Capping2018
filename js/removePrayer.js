function removePrayer(id) {
    var button = document.getElementById('removePrayer--'+id)
    button.click();
}

function removeComment(id){
    var button = document.getElementById('removeComment--'+id)
    button.click();
}